# ───────────────────────────────────────────────
#  .gitignore — site Antonin Atger
#  Liste blanche : SEULES ces 4 pages sont versionnées
#  et déployées. Tout le reste est ignoré (mais conservé
#  sur le disque local).
# ───────────────────────────────────────────────

# Tout ignorer par défaut...
*

# ...sauf le .gitignore et les 4 pages à déployer
!.gitignore
!collaborations.html
!couverture-mediatique.html
!interventions.html
!publication.html
